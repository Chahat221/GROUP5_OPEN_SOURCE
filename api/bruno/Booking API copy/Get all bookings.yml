info:
  name: Get all bookings
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:5001/bookings
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
