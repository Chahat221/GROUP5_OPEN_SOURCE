info:
  name: Update booking status
  type: http
  seq: 6

http:
  method: PUT
  url: http://localhost:5001/bookings/BOOKING_ID/status
  body:
    type: json
    data: |-
      {
        "status": "accepted"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
