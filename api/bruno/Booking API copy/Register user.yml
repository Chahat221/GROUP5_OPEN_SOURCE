info:
  name: Register user
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5001/register
  body:
    type: json
    data: |-
      {
        "name": "Sania",
        "email": "sania@test.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
