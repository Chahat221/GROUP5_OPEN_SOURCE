info:
  name: Login User
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5001/login
  body:
    type: json
    data: |-
      {
        "email": "sania@test.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
