info:
  name: Get all events
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:5001/events
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
