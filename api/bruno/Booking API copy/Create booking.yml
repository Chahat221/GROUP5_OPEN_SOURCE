info:
  name: Create booking
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:5001/bookings
  body:
    type: json
    data: |-
      {
        "userId": "1",
        "service": "haircut",
        "date": "2026-04-20",
        "startTime": "10:00",
        "endTime": "11:00"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
